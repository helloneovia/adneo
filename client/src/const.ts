export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Retourne l'URL de la page de connexion locale
export const getLoginUrl = (_returnPath?: string) => "/login";

// Retourne l'URL de la page d'inscription locale
export const getRegisterUrl = () => "/register";
